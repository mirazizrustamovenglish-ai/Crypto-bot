[phases.setup]
nixPkgs = ['python311', 'gcc']

[phases.install]
cmds = ['pip install --upgrade pip', 'pip install -r requirements.txt']

[phases.build]
cmds = ['echo "Build complete"']

[start]
cmd = 'python crypto_signal_bot_advanced.py'
